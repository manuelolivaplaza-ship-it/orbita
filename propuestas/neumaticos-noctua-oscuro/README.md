# NOCTUA

Atelier de neumáticos. Sitio oscuro, nocturno y preciso. Contenido en español de Chile.

```bash
cd web
npm install
npm run dev
```

Rutas: `/` · `/catalogo` · `/catalogo/[slug]` · `/medida` · `/taller` · `/noctua` · `/cita`
