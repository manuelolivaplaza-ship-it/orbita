# Alba — odontología de autor

Sitio de la clínica **Alba Atelier Dental** (Salamanca, Madrid). Next.js, Tailwind, shadcn/ui.

```bash
npm install
npm run dev
```

Abre [http://localhost:3000](http://localhost:3000).

Las solicitudes de cita se guardan en `data/citas.json`.
